export interface Secrets {
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_HOST: string;
  DB_PORT: number;
  DB_NAME: string;
  SMTP_USERNAME?: string;
  SMTP_PASSWORD?: string;
  SMTP_HOST?: string;
  SMTP_PORT?: number
}

